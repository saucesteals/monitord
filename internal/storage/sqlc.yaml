version: "2"
sql:
  - engine: "sqlite"
    schema: "migrations"
    queries: "queries"
    gen:
      go:
        package: "db"
        out: "db"
        emit_interface: true
        emit_empty_slices: true
        emit_exact_table_names: false
