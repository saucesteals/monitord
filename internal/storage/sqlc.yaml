version: "2"
sql:
  - engine: "sqlite"
    schema: "schema.sql"
    queries: "queries"
    gen:
      go:
        package: "db"
        out: "db"
        emit_interface: true
        emit_empty_slices: true
        emit_exact_table_names: false
        omit_unused_structs: true
